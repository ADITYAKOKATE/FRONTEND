{
  "name": "nasa-bioexplorer",
  "version": "1.0.0",
  "description": "AI-powered dashboard for exploring NASA biology research papers",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["nasa", "biology", "research", "ai", "dashboard"],
  "author": "NASA BioExplorer Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
